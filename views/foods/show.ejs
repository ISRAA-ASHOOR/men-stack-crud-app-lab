<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= food.name %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= food.name %></h1>
    <% if (food.isYourFav) { %>
        <p>This food is your favorite!</p>
      <% } else { %>
        <p>This food is not your favorite!!</p>
      <% } %>
      <div class="food-actions">
      <form class="delete-form" action="/foods/<%=food._id%>?_method=DELETE" method="POST">
        <button class="delete-button" type="submit">Delete <%= food.name %></button>
      </form>
      <a class="edit-link" href="/foods/<%= food._id %>/edit"> <div class="edit-text">Edit <%= food.name %></div></a>
      </div>
      <a class="foods-index-link" href="/foods/">
        <div class="index-link-text">Back to Foods</div>
        </a>
  </body>
</html>